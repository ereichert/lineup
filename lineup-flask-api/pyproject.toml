[project]
name = "lineup-flask-api"
version = "0.1.0"
description = "API for experimental lineup management application."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "black>=25.1.0",
    "pre-commit>=4.2.0",
    "ruff>=0.11.8",
]
